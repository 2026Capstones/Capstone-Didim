spring:
  ai:
    openai:
      base-url: http://203.255.224.60:4000
      api-key: "${AI-KEY}"
      chat:
        options:
          model: yc-gemma-4-26b-a4b
          temperature: 0.0
      embedding:
        enabled: false
    embedding:
      transformers:
        enabled: true

# 추가: OpenAI 공식 API 설정 (Audio 전용)
  openai:
    api:
    key: ${OPENAI-KEY} # properties 파일에서 읽어옴
    base-url: https://api.openai.com/v1